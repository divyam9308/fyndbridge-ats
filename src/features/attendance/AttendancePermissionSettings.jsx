import { useState } from 'react'
import { ShieldCheck } from 'lucide-react'
import { PERMISSION_ITEMS } from './attendanceData'

export default function AttendancePermissionSettings(){const [values,setValues]=useState(()=>{try{return JSON.parse(localStorage.getItem('fb-attendance-permissions'))||{}}catch{return {}}});const change=(key,value)=>{const next={...values,[key]:value};setValues(next);localStorage.setItem('fb-attendance-permissions',JSON.stringify(next))};return <section className="admin-section att-admin-permissions"><div className="admin-section-header"><div className="admin-section-title-wrap"><div className="admin-section-icon"><ShieldCheck size={21}/></div><div><h2>Attendance Permissions</h2><p>Choose which administrator role can use each Attendance &amp; Leave capability.</p></div></div></div><div className="admin-section-body"><div className="att-permission-list">{PERMISSION_ITEMS.map(([key,title,description])=><div className="att-permission-row" key={key}><div><h3>{title}</h3><p>{description}</p></div><div className="att-segment"><button className={(values[key]||'admins')==='admins'?'is-active':''} onClick={()=>change(key,'admins')}>Admins</button><button className={values[key]==='super_admins'?'is-active':''} onClick={()=>change(key,'super_admins')}>Super Admins</button></div></div>)}</div></div></section>}
